# EchoMusic 酷狗奖励

面向 EchoMusic `>=2.2.8-beta.7` 的插件，对齐 [develop202/kgcheckin](https://github.com/develop202/kgcheckin) 的概念 VIP 每日领取流程：

1. **听歌回执领取**（`/youth/v2/report/listen_song` + 可选领取接口）
2. **广告播放自动上报**（`/youth/v1/ad/play_report`，最多 8 次，默认间隔 30 秒）
3. **VIP 状态查询**（`kugouvip.kugou.com/v1/get_union_vip`）

将整个 `echomusic` 文件夹放进 EchoMusic 插件目录，刷新并启用。插件只复用当前登录态，不保存 token、userid 或 dfid。

## 功能按钮

| 按钮 | 说明 |
| --- | --- |
| 一键每日任务 | 听歌领取 → 广告上报 → 查询 VIP（与 kgcheckin 主流程一致） |
| 听歌奖励 | 仅执行听歌回执 + 领取 |
| 广告领取 | 仅执行广告 play_report 循环 |
| 签到领取 | 仅调用 `/youth/v1/recharge/receive_vip_listen_song` |
| 查询 VIP | 查询概念 VIP 到期时间 |

## 设置项

- **启动后自动执行每日任务**：当天尚未成功跑过时，启动约 1.5 秒后自动执行一键每日任务
- **每日任务中包含广告播放上报**：关闭后一键/自动任务会跳过广告步骤
- **广告次数 / 间隔秒数**：默认 `8` 次、`30` 秒，与上游一致
- **MixSongID**：可留空；优先用输入框 → 当前播放歌曲 → 默认 `666075191`（与上游一致）

## 0.3.0

- 接入 kgcheckin 广告播放上报：`POST /youth/v1/ad/play_report`（`ad_id=12307537187`，模拟 30 秒播放）
- 广告最多 8 次循环，成功后等待间隔再领；`error_code=30002` 视为今日次数用尽
- 新增「一键每日任务」：听歌 + 广告 + VIP 查询，自动签到改为走完整每日流程
- 听歌 MixSongID 默认回退到上游 `666075191`；听歌领取失败不阻断广告步骤
- 可配置广告次数与间隔；UI 增加进度状态与更完整日志

参考上游：`develop202/kgcheckin`（`main.js` + `api/module/youth_listen_song.js` + `youth_vip.js`）。

## 0.2.0

- 同步 `develop202/kgcheckin` 2026-07-14 最新代码中的酷狗请求参数：概念版客户端版本更新为 `11436`，并采用新的 39 位 `mid` 与对应 `uuid` 算法。
- 同步默认 Android 请求标识，以及网络失败最多 3 次重试。
- 听歌接口返回 `130012` 时明确显示“今日已领取”，VIP 到期时间优先读取最新返回结构。
- 保留 EchoMusic 当前登录态与高对比日志界面。

参考上游提交：`develop202/kgcheckin@21e8814`（2026-07-14）。

## 0.1.1

- 修复浅色弹窗中文字和日志几乎不可见的问题。
- 日志改为固定深色高对比面板，并增大字号、行高和内边距。
- 接口没有错误说明时，日志会显示实际状态码。

## 说明与免责

本插件仅调用官方活动相关接口，供学习研究。请遵守当地法律法规与酷狗服务条款，勿用于商业或违法用途。音乐平台不易，请支持正版。
